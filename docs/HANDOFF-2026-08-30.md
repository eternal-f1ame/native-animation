# Session Handoff — 2026-08-30

Context bootstrap for a fresh session picking up this work. Read in order:
workspace `../CLAUDE.md` → repo `CLAUDE.md` (cluster conventions — read before touching SLURM) → this file → `docs/roadmap.md` and the plan status lines in `docs/superpowers/plans/`.

## Where things stand

**Method (v2): code complete.** All modules TDD'd green (~85 tests; gate: `TEST_TARGETS=tests sbatch scripts/slurm/run_tests.sbatch`). Memory smokes done: CT-a shape (256×448×17) fits 4×80GB under FSDP; CT-b (480×832×49) needs 8×80GB and/or `offload_models` for T5+VAE + FSDP CPU offload. Configs `ct_a/ct_b/sft.yaml` load weights fully locally (`model_paths` + `tokenizer_path`).

**Stage-0 corpus pipeline: RUNNING as a SLURM dependency chain.** Snapshot = 281 tars (~2.2T, ~150k posts). As of this handoff: ~195/281 stream tars complete, split array fully done (64/64), ~410k+ shot records in `shots/manifests/`, ~230+ shot packs (~400G) in `shots/packs/`, ~170 sidecar JSONLs in `clips/sidecars/`.

Chain (job IDs): stream arrays `360191` (drained) + retries `360754`, `360806` (**c1-7 excluded** — that node's local disk is full; SLURM can't see it, `TmpDisk=0`) → catchup `360755` (re-runs any straggler/failed tar; idempotent) → profile `360194` (+resume `361774` for 13 timed-out/failed shards) → **annotate `360302`** (GPU ×8, `comfy` env + anno-overlay, transformers backend, ~8s/shot — runs for days) → **squash `361775`** (**ON USER HOLD** per the user, 2026-08-31 — do not `scontrol release 361775 361776` until the user says so; `BATCH_SIZE=8`; runs only after annotation + profiling — it deletes consolidated packs, so it must NEVER overlap pack readers) → accounting `361776`. Failed stream tasks are normal-ish: cause has always been node-local-disk ENOSPC; catchup sweeps them. All sbatches now carry TERM-safe staging traps + a >4h orphan sweep.

**Storage:** object quota (2M files/user, pool-wide — home and datasets area are ONE filesystem) is the binding constraint; ~1.61M used after reclamation (was 1.95M). The pack/squash tiers (`docs/dataset.md`) keep the corpus's object footprint ~constant. Bytes are a non-issue (2.3T/7T).

## What to do, in order

1. **Babysit the chain to completion.** `squeue --me`; `sacct -j 360754,360806,360755,360194,360302,360756,360196 --format=JobID,JobName,State,ExitCode -nX`. New stream FAILED tasks → check `.err` for ENOSPC; if a new node is implicated, `--exclude` it (see repo CLAUDE.md) — catchup re-runs stragglers regardless. Use dependency chains for any new orchestration (background watchers get killed).
2. **When squash `361775` completes:** verify the `shots/shots_NNNN.sqsh` images (`unsquashfs -l` sample vs. a manifest record; each wave is a FRESH image — mksquashfs append corrupts colliding series dirs, never append), confirm consolidated packs were deleted and `_state_squash.json` lists packs + images. Readers mount every image via `scripts/slurm/lib/mount_shots.sh`. `shots_0000.sqsh` carries harmless unreachable `<series>_1` cruft from the one aborted append. Re-run the consolidator for later waves — single-writer, never concurrent with itself OR with profile/annotate jobs.
3. **When annotation `360302` finishes:** sample caption quality (3-section directives: tag/summary/description), then **Plan 1 T11–T12**: rebuild v2 metadata (tiers S/A/B; `rating==Safe` filter) and freeze the benchmark split. See `docs/superpowers/plans/2026-08-28-stage0-data-pipeline.md`.
4. **Launch CT-a:** `STAGE_CONFIG=configs/ct_a.yaml sbatch scripts/slurm/train_v2.sbatch`.
5. **Next implementation block — Plan 3** (evaluation suite: JEDi, AniSora-948 protocol, anchor-fidelity/flicker metrics; then GT-anchored DPO): `docs/superpowers/plans/2026-08-28-evaluation-dpo-v2.md`. Not started.

## Held / optional

- **Delta scrape** (`delta_scrape.sbatch`, ~25k newer posts): HELD — its scraper writes loose files; pack its output first or get the quota raised. `cf_cookies.json` at the datasets root is its auth; never enters git.
- Admin ask: object quota 2M → 5M (one email; softened by pack/squash but still the clean fix).
- CT-b after CT-a, with the memory levers above.

## Rules that have bitten before (do not relearn)

- Framing: native animation leads; flow matching is substrate. AniMatrix = concurrent corroboration, cite openly, never defensively.
- Never redistribute Sakugabooru media — release is post IDs + metadata + scripts.
- `comfy` env is sacred: no torch/transformers upgrades; overlays with `--no-deps` only. Other conda envs / research dirs / `~/.cache/huggingface` belong to the user's other projects — never touch without the user naming them.
- The auto-mode classifier blocks bulk deletes outside the workspace: attempt granular commands, else hand the user a `!`-prefixed command.
- SLURM spools sbatch at submit; `tools/*.py` are read at run time. Patch scripts by line-index and verify by eye (`str.replace` no-ops shipped broken sbatches twice).
- Login node: no heavy imports, no IO-heavy loops while data jobs run; tests go through `run_tests.sbatch`.
